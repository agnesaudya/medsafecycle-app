package com.example.medsafecycle.config

import android.content.Context

object Injection {
//    fun provideRepository(context: Context): StoryRepository {
//        val database = StoryDatabase.getDatabase(context)
//        val apiService = ApiConfig.getApiServiceNoToken()
//        val userPreference = UserPreference(context).getUser()
//        return StoryRepository(database, apiService, "Bearer ${userPreference.token.toString()}")
//    }
}