<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".SecondLandingFragment"
    android:background="@drawable/landing_2_bg">

    <!-- MedSafe Cycle -->
    <TextView
        android:id="@+id/medsafe_cyc"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginStart="64dp"
        android:layout_marginTop="156dp"
        android:gravity="center_horizontal|top"
        android:text="@string/medsafe_title"
        android:textAppearance="@style/medsafe_title"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- welcome-msg -->
    <TextView
        android:id="@+id/mudah_digun"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="64dp"
        android:layout_marginTop="8dp"
        android:gravity="top"
        android:text="@string/welcome_msg"
        android:textAppearance="@style/welcome_msg"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/medsafe_cyc" />


</androidx.constraintlayout.widget.ConstraintLayout>